# Redis 基础配置
bind 0.0.0.0
protected-mode yes
port 6379

# 持久化配置
appendonly yes
appendfilename "appendonly.aof"

# 内存配置
maxmemory 512mb
maxmemory-policy allkeys-lru

# 日志配置
loglevel notice
logfile ""

# 连接配置
timeout 0
tcp-keepalive 300
